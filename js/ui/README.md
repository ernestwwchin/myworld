UI Folder Structure

- templates.js: HTML template helpers.
- action-buttons.js: dynamic action-button visibility and state helpers for GameScene.
- core-ui.js: global UI helpers (stats panel toggle, dice click handling, etc.).

Guideline:
- Keep GameScene focused on gameplay state.
- Keep DOM structure manipulation in ui/ modules.
