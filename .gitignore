# Tofu/Terraform state — stored in S3, never commit locally
tofu/terraform.tfstate*
tofu/.terraform/

# Secrets — never commit these
*.secret.*
.sops.yaml

# AI agent config — may contain infrastructure identifiers
.claude/

test-results/
playwright-report/
node_modules/
assets/*.zip
assets/_downloads/
assets/_staging/
**/_MACOSX/
**/.DS_Store
