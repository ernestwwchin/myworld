[extend]
useDefault = true

[allowlist]
description = "ignore encrypted and non-sensitive files"
paths = [
  '''\.secret\.(yaml|env|json|yml)$''',  # SOPS encrypted files (matches .sops.yaml path_regex)
  '''\.gitleaks\.toml''',                # this file
]
